info:
  name: Logar como Funcionário
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/auth/login"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
